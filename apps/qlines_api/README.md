# QLines API

NestJS API for the QLines queue-management platform.

## First run

```powershell
pnpm install
pnpm start:dev
```

Then open `http://localhost:3000/api/v1/health`.

## Checks

```powershell
pnpm lint
pnpm test
pnpm build
```
